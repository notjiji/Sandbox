[pytest]
testpaths = tests app
pythonpath = .
python_files = test_*.py *_test.py tests.py lifecycle_tests.py
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: API or database integration tests
    docker: Requires Docker Compose (CI quality-gate jobs / make staging-acceptance / make backup-integration-test)
